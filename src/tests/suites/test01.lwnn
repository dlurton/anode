#!/home/dave/projects/lwnn/bin/lwnn

1; 2; 3;


func foo:int(bar:int) bar * 10;


assert(foo(1) == 10);
assert(foo(2) == 20);
assert(foo(3) == 30);

# we're just going to allow this to fail for now--the next several commits should flesh out some of these test suites a bit better.
assert(foo(4) == 31);

